# Scala + Google Cloud Pub/Sub Hello World

Goals:

- Having a Scala Stream Collector publishing to a GCP Pub/Sub topic
- Having a GCP Dataflow pipeline reading from that topic and performing some operation on it
- Storing the pipeline's output on GCP BigQuery

(dummy tracker will publish continuosly to the SSC)
